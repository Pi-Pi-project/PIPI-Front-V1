import styled, { createGlobalStyle } from "styled-components";

export const Container = styled.div``;

export const BoardWriteGlobalStyle = createGlobalStyle`
  body {
    background-color: #f8f9fc;
  }
`;
